# 长沙理工大学模板版论文

本目录基于 `csust-latex-sig/CSUSTBachelorThesis` 主分支提交
`82f51a94b4813926134ebd87a65badcb1f9aa859` 适配。

编译命令：

```bash
make thesis
```

输出文件为 `build/thesis.pdf`。正式提交前需要在 `baseinfo.tex` 中补全
学号、班级和指导教师，并向学院确认当届封面与装订规范。
